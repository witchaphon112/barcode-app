import React from 'react';

export default function AuditLog() {
  return (
    <div style={{ maxWidth: 900, margin: '0 auto', padding: 24 }}>
      <h2>Audit Log (บันทึกกิจกรรมสำคัญ)</h2>
      {/* ตาราง log, filter, export */}
      <div>ฟีเจอร์: ดู log, filter, export log</div>
    </div>
  );
} 